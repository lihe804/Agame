# 海滨小镇 · 3D 角色漫游游戏 — 交接文档

> 最后更新：2026-09-16 ｜ 状态：**阶段性完成，可运行；主分支基线 `2bcdc13`；分享包需按当前源码重新生成**

---

## 1. 项目概述

网页端 3D 角色扮演漫游游戏（Three.js 0.160，无构建工具、原生 ESM）。
两名按真实比例建模的角色（刘世锦 183cm / 王梓航 150cm）在海滨小镇中自由漫游。
当前包含**五套注册表驱动的关卡**：

- 深海远征、环屋跳高：训练短关，保留终点、独立计时和各自独立的落水救援。
- 登天云梯：500 台向高空延伸，20 个阶段宝箱，最高约 218m。
- 潮汐远征：500 台横向折返航路，20 段、20 个阶段宝箱，实际航程约 1454m；含窄台、旋转阵列和 20 种动态机制。
- 星跃追光：500 台远海高空星轨，20 个阶段宝箱、80 个充能台和 140 处强制冲刺断层。

三个 500 台长关均按 25 台分段、独立保存积分与检查点，可用 HUD 按钮或 `T` 键按
“登天云梯 → 潮汐远征 → 星跃追光 → 自由模式”循环切换。只有深海远征和环屋跳高保留
训练计时；三个正式长关不启动、不显示计时。自由模式隐藏三个长关，可自由训练两个短关，
且不会写入积分、宝箱、检查点或 `lastCourse`。旧的前三名榜单功能已移除，不再读写 `wb-board-*`。

### 当前交接基线

- GitHub：`https://github.com/lihe804/Agame`
- 远程默认分支：`main`
- 当前基线提交：`2bcdc13`（“修复了潮汐远征关卡的一些bug”）
- 分支状态：本地 `main`、`origin/main`、`origin/HEAD` 均指向 `2bcdc13`
- `feature` 已合并进 `main`，远程 `feature` 已删除；后续开发直接从 `main` 建新分支
- 更新前工作区干净，本次仅修改本文档；`release/` 为 `.gitignore` 忽略的生成目录，当前 checkout 中不存在现成分享包

- 开发运行：`python tools/serve.py 8765 D:/code/Agame/web` → 打开 `http://127.0.0.1:8765/`
- 分享运行：解压 `release/云端小镇-分享版.zip`，双击 `启动游戏.cmd`
- ⚠️ 源码必须以 `web/` 为准；`release/cloud-town-share/game/` 只是可发布快照
- ⚠️ `serve.py` 带 `no-store, no-cache` 头，但仍建议 **Ctrl+F5** 强刷
- 操控（键盘）：WASD 移动 / 空格跳 / Shift 跑 / F 星跃 / E 开门 / R 返回长关存档或自由训练起点 / T 切换关卡模式 / M 静音 / 鼠标点击锁定视角 / 滚轮缩放
- 操控（手柄，自动连接）：左摇杆移动 / A 跳 / B 星跃 / LT 或 L1 加速 / 右摇杆视角；键盘可同时使用

## 2. 目录结构

```
D:\code\Agame\
├── HANDOVER.md            ← 本文档
├── PLAN.md                ← 早期里程碑规划（历史参考）
├── release\               ← 本地生成的发布目录（已被 .gitignore 忽略）
│   ├── cloud-town-share\  ← Windows 解压即玩目录
│   │   ├── game\          ← web 的发布快照（不在此处直接开发）
│   │   ├── 启动游戏.cmd
│   │   ├── serve.ps1      ← 零依赖本地 HTTP 服务，兼容 PS 5.1/7
│   │   └── README.txt
│   └── 云端小镇-分享版.zip
├── web\                   ← 全部运行时代码（根目录即站点根）
│   ├── index.html / style.css
│   ├── vendor\            ← three.js 本地化（three.module.js + jsm\），无 CDN 依赖
│   ├── models\            ← char1.glb / char2.glb（AI 生成，碎片化网格）
│   └── src\
│       ├── main.js        ← 入口：渲染器、主循环、积分、分关存档、救援与交互接线
│       ├── scene.js       ← 场景组装 + 地形高度场 + 关卡注册表 COURSES + 关卡视觉工厂
│       ├── player.js      ← 角色控制器（移动/跳跃/纯实体平台碰撞/第三人称相机）
│       ├── rig.js         ← GLB 网格 → SkinnedMesh 伪骨骼（当前 ANIM_MODE 未启用）
│       ├── config.js      ← 角色/调色板/ANIM_MODE 开关
│       └── sfx.js         ← WebAudio 程序化音效（无外部素材）
├── tools\
│   ├── serve.py           ← 开发用无缓存静态服务器
│   ├── gen3d.py           ← 角色模型生成辅助工具
│   ├── inspect_glb.py     ← GLB 解析器（查碎片化程度/连通块）
│   ├── cdp-check.mjs      ← 基础 CDP 端到端脚本
│   ├── cdp-sky-check.mjs  ← 登天/潮汐专项验收
│   ├── cdp-comet-check.mjs ← 星跃追光专项验收
│   ├── cdp-switch-check.mjs ← 四模式切换、自由模式隔离与移动端 HUD 验收
│   └── cdp-perf-check.mjs ← 三长关绘制调用/帧时间采样
├── assets\models\         ← 角色模型源文件与迭代版本
└── 人物\                  ← 角色参考照片（**仅限家庭本地使用，勿外传**）
```

`release/` 已列入 `.gitignore`，不会进入源码仓库；修改 `web/` 后必须重新同步并打包，
否则发布快照仍是旧版本。当前 checkout 未包含 `release/cloud-town-share/` 或
`release/云端小镇-分享版.zip`，需要分享时按下文流程从当前 `web/` 重新生成。

## 3. 核心系统速览

| 系统 | 位置 | 要点 |
|---|---|---|
| 地形高度场 | `scene.js terrainHeight()` | 分析函数，地形网格与角色落地共用同一来源 |
| 世界底海 | `scene.js buildBeachScene()` | 6000×6000 不透明深海面（y=-0.5），陆地任意方向入海 |
| 角色控制器 | `player.js Player` | AABB 碰撞 + 地形落地 + 纯实体平台（见下） |
| 平台碰撞 | `player.js ALL_PLATFORMS` | **真 3D 盒**：身体与台体体积相交才阻挡；顶面落地走 `groundAt` 单向判定 |
| 相机 | `player.js CameraRig` | 防穿墙射线 + 俯仰钳制（-0.2~0.95） |
| 角色动画 | `player.js + config.js ANIM_MODE` | 当前 `'rigid'` 整体刚体动画（起伏/压扁），无网格变形 |
| 音效 | `sfx.js` | WebAudio 合成，M 键静音 |
| 登天云梯 | `scene.js` + `main.js` | 500 台分 20 段；120 个 orbit/lift/shuttle 动态台；房屋区域全高度禁飞；旋转阶梯不套 24×4 线框笼（笼体挡视线，仅 `ocean` / `comet` 保留） |
| 潮汐远征 | `scene.js` + `main.js` | 500 台折返海路、20 种分段机制、325 个动态台；首两台固定并保留地形净空；未开本段宝箱时后段平台不可落地 |
| 星跃追光 | `scene.js` + `main.js` + `player.js` | 500 台远海高空曲线；每段 7 个长断层，必须按 `F`/手柄 B 消耗星跃跨越；80 个充能台 |
| 积分、分关存档与切换 | `main.js` + `index.html` | 三个长关独立保存；`T`/按钮按长关加自由模式循环；HUD 一键刷新全部积分、宝箱、检查点和星跃次数；自动迁移旧云梯存档 |
| 训练计时 | `main.js` + `index.html` + `style.css` | 仅 `sea` / `height` 启动；起点必须真正站在起点台面上（`supportPlatform` 同一对象）；落到目标显示用时；只有真正落到本关以外的地面/台面才取消本轮（空中下落不取消）；深海落水回起点并清零重新计时；正式长关切换和救援会强制停止计时 |
| 终极大奖 | `scene.js makeGrandPrize()` + `main.js` | 仅三个长关（`sky`/`ocean`/`comet`）的终点：不再是宝箱，而是一个惊喜礼盒：靠近约 11m 开始颤抖/冒火星/光环加速，踩到终点后开盖、金色大奖升起、光柱冲天、三波烟花 + 冲击波；配色按关卡 id 区分；读档时直接定格为“已揭晓”。练习关 `sea` / `height` 保留原终点宝箱 |
| 自由模式 | `main.js` + `scene.js` | 隐藏 `sky` / `ocean` / `comet`，保留 `sea` / `height`；不启动长关、不写长关存档，`R` 回自由训练起点 |
| 世界边界 | `main.js` + `player.js` | 根据全部关卡及动态台扫掠范围计算 `Player.limits`；后备边界同步扩到 `zMin=-3012`，避免远海平台被旧边界截断 |
| 手柄 | `player.js createInput().updateGamepad()` | 主循环每帧轮询 `navigator.getGamepads()`；左摇杆径向死区 0.12 + 幅度控速（`input.mx/my` 模拟量，Player.update 与键盘开关量叠加）；A=跳（buttons[0]）、L2/L1=加速（buttons[6]/[4]）、右摇杆=视角（axes[2]/[3]，径向死区 0.12，全杆偏航 ≈144°/s，增量按秒折算并入 dragX/dragY）；键盘与手柄状态每帧合并（`jumpKey||jumpPad`），键盘 keyup 不会误清手柄按住的状态 |
| 平台空间索引 | `scene.js` | 静态平台按 4m 网格分桶，动态台单独查询，避免 1500+ 台长关逐帧全量扫描 |
| 性能 | `scene.js` 末尾 + `main.js` | 见 §6 |

## 4. ⭐ 关卡扩展指南（核心接口）

**所有关卡由 `scene.js` 顶部的 `COURSES` 注册表驱动。新增一关 = 加一个定义对象，其余全自动：**
碰撞（纯实体）、落地判定、宝箱/旗子/彩带、通关提示和分关进度接线——无需改动
player.js / main.js 的核心逻辑。

### 关卡定义字段

```js
// scene.js → COURSES 数组中追加：
{
  id: 'my-level',                    // 唯一 id
  name: '我的关卡',                   // 通关提示显示名
  style: 'float',                    // 'sea' | 'float' | 'sky' | 'ocean' | 'comet'
  platforms: [                       // platforms[0] = 路线起点（用于识别当前关卡）
    { x: 10, z: 30, top: 2.0, half: 0.85 },
    { x: 12, z: 32, top: 2.4, half: 0.85, prop: 'palm' },  // prop:'palm'=棕榈树冠跳台
    { x: 14, z: 34, top: 2.8, half: 0.5, motion: { type:'lift', baseTop:2.8, amplitude:0.08, speed:1.2, phase:0 } },
    { x: 17, z: 35, top: 3.2, half: 0.95, reward: { id:'stage-1', stage:1, points:250 } },
    // ...
    { x: 20, z: 40, top: 6.0, half: 2.4 },                 // 半宽>2 自动用金边终点台样式
  ],
  goal: { x: 20, z: 40, top: 6.0, half: 2.4 }            // 终点（靠近宝箱通关）
}
```

### 布点硬约束（违反会重叠/撞墙/跳不过）

1. **普通相邻平台中心距通常为 [2.8, 3.2]**：需同时检查台体扫掠范围，不能只看中心距。
   潮汐窄台允许约 2.1~3.6m，但必须实测动态扫掠不重叠且落点可达；仅星跃强制断层可用 5.6~6.0
2. **每级升幅 ≤ 0.65**（跳跃顶点 0.94m）
3. 间隙（中心距 − 两台 half 之和）≤ 2.0 = 步行跳极限；>2.3 必须助跑跳（Shift）
4. 平台不要落在其他关卡终点/房屋 footprint 内（实体会互相干扰）
5. 长关必须有防捷径分段：完成当前 25 台段宝箱前，下一段实体碰撞保持锁定
6. 星跃强制断层必须沿当前行进方向设置可见落点，并在前后提供充能台；不能只靠隐藏碰撞

### 进度、存档与救援规则（自动生效）

- 真正踩上 `sea` / `height` 的 `platforms[0]` 台面会启动对应训练计时（站在起点台旁边的沙地上不算，避免“计时刚亮就灭且无法再启动”）；踩上三个正式长关起点只切换并记录路线，不启动计时
- 靠近阶段宝箱并开箱 → 增加积分、播放彩带/音效并更新对应关卡检查点
- 不同关卡落水与坠落救援互不复用：
  `sea` 回深海远征起点，`ocean`/`comet` 回各自检查点，避免与第一关逻辑冲突
- 深海远征落水会回到远征起点并把本轮计时清零重启；真正落到本关以外的地面/台面（着地瞬间 `vy` 归零）才取消本轮计时，起跳/走下台沿的空中下落不再误判为“离开台阶”；环屋跳高从高空落回地面会取消计时；到达训练终点显示本次完成用时，离开终点后重新武装、可反复挑战并再次结算
- 长关复活按“检查点台面高度与地形高度取较大值”计算出生点，首台也不得埋入地形；登天云梯和潮汐远征起点台会随所在地形自动抬升
- 长关阶段宝箱：每 25 台一座，打开后自动加分并更新对应关卡检查点
- 终极大奖：仅三个长关（`sky` / `ocean` / `comet`）的终点放一座“惊喜礼盒”（`scene.js makeGrandPrize()`；练习关 `sea` / `height` 不走这条分支，仍由 `makeCourseVisual()` 生成原终点宝箱）。玩家进入约 11m 内开始颤抖、冒金色火星、光环加速（蓄力预兆）；踩到终点后盒盖掀开、金色大奖升起、光柱冲天、三波烟花与冲击波，并按关卡 id 换配色。`rewards` 里最后一个（`final`）仍作为终点奖励数据（积分/存档/检查点不变，长关终点台不再有宝箱实例）；读档恢复时直接定格为“已揭晓”，不放烟花
- 存档：`wb-course-progress-v2` 分别保存登天云梯、潮汐远征、星跃追光；宝箱记录阶段/高度/台号
- 星跃状态：每次冲刺消耗 1 格（上限 3），落到发光充能台或打开本段宝箱补满
- 关卡切换：`T` 或 HUD 按钮按“登天 → 潮汐 → 星跃 → 自由模式”循环，切换只载入目标关检查点，不覆盖其他关卡存档
- 自由模式：三个长关的视觉、碰撞、奖励和救援均不参与；两个训练短关仍可完整游玩和计时，离开自由模式后本轮计时停止
- 一键刷新：按钮清空总积分、三个长关的宝箱和检查点，并把当前关卡恢复为登天云梯
- 动态台：`motion.type` 支持 `orbit`（绕轴旋转）/ `lift`（升降）/ `shuttle`（横向往返）/ `wave`（波浪漂移）

### 更大的扩展点

- **新道具跳台**：`makeCourseVisual()` 里仿照 `prop === 'palm'` 分支加类型（如岩石顶、屋顶）
- **新平台样式**：`def.style` 分支（现 `'sea'`/`'float'`/`'sky'`/`'ocean'`/`'comet'`）内加视觉变体
- **新长关机制**：星跃的 `dashPad`、`dashRim` 和 `F` 键逻辑是专用扩展，不应仅靠注册表假设自动生效
- **角色**：`config.js CHARACTERS` 加条目 + GLB 放入 `web/models/`；`ANIM_MODE` 详见 §5

## 5. 已知限制与重要注意事项

1. **模型是碎片化网格**（inspect_glb.py 实测 1339 块互不连通面片）——这是历次
   “撕裂/黑丝/残影”问题的总根源。顶点蒙皮在此类网格上无解，故角色动画走整体刚体方案。
   **若未来换用带正规骨架的模型（如 Mixamo 重定向）**：`config.js` 把 `ANIM_MODE` 改为
   `'limb'` 即可启用 `rig.js` 伪骨骼摆臂摆腿（代码保留未删）。
2. **地面行走会碰撞低空平台**：平台是实体，悬浮低于 1.9m 的台体在地面走位时会被阻挡——
   这是“纯实体”的物理代价，布新关卡时把低空台放在路线附近而非步行道上。
3. **`web/` 是唯一源码目录**：不要直接修改 `release/cloud-town-share/game/`。发布目录是生成快照，
   源码更新后必须重新同步并重打包。
4. **浏览器验收依赖 CDP 9222**：专项脚本位于 `tools/`，需要 Chrome/Chromium 以可写 profile 启动并开放调试端口。
5. **Node 版本差异**：当前环境 Node 24 的原生 WebSocket 连 CDP 曾出现兼容问题。脚本连不上时，
   改用 Node 22 或等价的 Playwright 回归流程，不要把页面加载失败误判为游戏故障。
6. **开发与分享启动不同**：开发使用 `tools/serve.py`；收件人使用发布包内的 `serve.ps1`。
   两种方式都必须通过 HTTP 访问，直接双击 `index.html` 会因 ESM/CORS 失败。
7. **角色照片隐私**：`人物/` 目录为真人参考照，仅限本地家庭使用，严禁上传/外传；发布包已排除该目录。

## 6. 性能优化清单（已做，改场景时请保持）

- 像素比封顶 1.75 + `powerPreference: 'high-performance'`
- 包围球 <0.45 的小物件不投影（`scene.js` 末尾遍历）
- 静态物体矩阵冻结（`matrixAutoUpdate=false`）；**动画对象必须标 `userData.animated = true`**
  （遍历会跳过它及其子孙——新加动效对象忘标记 = 动画失效）
- 浪面法线每 3 帧重算；涟漪/彩带/扬尘均为对象池
- 三个 500 台长关的静态台、动态台和装饰均用 `InstancedMesh` 合批，只更新动态实例矩阵
- 非当前长关整体隐藏，避免把另外两张 500 台场景同时送进渲染管线
- 静态平台按 4m 空间网格分桶，角色查询只扫描附近格子，不再逐帧遍历 1500+ 平台
- 最近一次 1600×900、DPR 1 的 CDP 采样：登天 33 calls、潮汐 218 calls、星轨 44 calls；
  当前无头环境使用 SwiftShader（软件渲染），绝对 FPS 不能代表真实独显表现；潮汐的 218 calls
  是当前性能热点，后续增加海面装饰时应优先复用实例或合并材质
- 潮汐远征按 20 段切换可见障碍和动态平台；旋转出口强制沿外法线前进两步，避免 U 型折返形成捷径
- ⚠️ 往场景里加“每帧都在动的东西”时，记得标 `animated`；加静态物体则什么都不用做

## 7. 调试入口

- `window.__wb` = `{ state, enterExplore, camera }`（控制台可手动切场景/看状态）
- `node tools/cdp-sky-check.mjs http://127.0.0.1:8765/ .tmp/shots/sky`：
  校验登天/潮汐的 500 台结构、各 20 宝箱、潮汐 20 种机制、动态台、折返防捷径、
  分关存档、`R` 载入、救援隔离、仅训练关计时和移动端 HUD
- `node tools/cdp-comet-check.mjs http://127.0.0.1:8765/ .tmp/shots/comet`：
  校验星跃 500 台、20 宝箱、80 充能台、140 断层、`F` 跨断层、落水复活和独立存档
- `node tools/cdp-switch-check.mjs http://127.0.0.1:8765/`：
  校验四模式切换顺序、自由模式隔离、训练计时、长关首台碰撞、切换不清档、星跃充能恢复和移动端按钮布局
- `node tools/cdp-perf-check.mjs http://127.0.0.1:8765/`：
  分别采样三个长关的 FPS、帧时间、draw calls、三角形和显存对象数量

## 8. 发布与分享

重新打包后的目标结构：

```text
release\cloud-town-share\
├── game\               ← web 的完整发布快照
├── 启动游戏.cmd        ← 用户双击入口
├── serve.ps1           ← 本地 HTTP 服务
└── README.txt

release\云端小镇-分享版.zip
```

- 当前工作区没有 `release/` 发布物；以下字段必须在新包生成后重新记录
- 新包最后打包时间：待生成
- 新包大小：待生成
- 新包 SHA256：待生成
- 上一版分享包（2026-09-15 13:54，SHA256
  `AC886DDCD3AC6960C9DFDDCC334B31F8AE52A88CD787B417B41B18AC611E37CB`）
  是旧源码快照，不包含后续计时调整、自由模式、远海边界修复、潮汐首两台固定和存档隔离修改
- 分享包不包含 Python/Node 依赖，也不包含 `人物/` 隐私照片

分享方式：

1. 直接发送 `release\云端小镇-分享版.zip`；对方解压后双击 `启动游戏.cmd`，保持命令行窗口开启。
2. 静态网站：把 `release/cloud-town-share/game/` 内的全部文件作为站点根目录上传到
   GitHub Pages、Cloudflare Pages、Netlify 或 Vercel。

上一版发布包回归记录（不能替代当前源码验收）：

- ZIP 可完整解压，顶层直接包含 `启动游戏.cmd`、`serve.ps1`、`README.txt`、`game/`
- `game/` 与原 `web/` 逐文件 SHA256 一致
- 包内首页、`src/main.js`、两个 GLB 均返回 200；路径穿越请求返回 403
- PowerShell 7、Windows PowerShell 5.1 以及 `启动游戏.cmd` 三条启动路径均实测可用

重新打包原则：

1. 先修改并验证 `web/`，不要修改发布快照。
2. 将 `web/` 的全部内容重新同步到 `release/cloud-town-share/game/`，删除已移除的旧文件。
3. 从发布目录启动一次，至少检查首页、脚本、模型和 `game/index.html` 不可直开时的 HTTP 行为。
4. 将 `release/cloud-town-share/` 下的 `game`、`启动游戏.cmd`、`serve.ps1`、`README.txt`
   重新压缩为 `release/云端小镇-分享版.zip`。
5. 重新计算 SHA256 并更新本节；旧校验值不能用于新包。

## 9. 版本里程碑（简）

M0-M2 角色建模与迭代 → M3 场景与漫游 → M4 肢体动画尝试（失败→刚体方案）→
M5 房屋交互 → 关卡 1 深海远征（零容错）→ 关卡 2 环屋跳高（悬空台阶）→
注册表重构 → 关卡 3 登天云梯（500 台、动态平台、20 阶段宝箱）→
关卡 4 潮汐远征重做（500 台折返航路、20 种机制、动态台、独立存档）→
关卡 5 星跃追光（500 台、有限星跃、充能台、强制断层）→
保留训练关计时并移除正式长关计时与榜单 → 四模式切换/自由训练/积分刷新/空间索引与渲染裁剪 →
潮汐远海动态世界边界修复 → 训练计时与自由模式存档隔离 →
潮汐远征首两台固定并清除地形穿插 → `feature` 合并进 `main`、切换默认分支并删除远程
`feature` → Windows 解压即玩分享包 → 训练计时修复 + 每关终点改为终极大奖
（惊喜礼盒 → 金色大奖 + 光柱 + 三波烟花；需按当前源码重新验收与打包）。

## 10. 后续接手建议

1. 以 `main` 的 `2bcdc13` 为交接起点；不要恢复已删除的 `feature` 分支，也不要继续在旧分支上开发。
2. 继续开发时从 `web/` 入手，优先扩充 `COURSES` 和对应验收脚本；不要维护两份手写源码。
3. 每次改动长关结构、存档键、计时或落水复活逻辑后，至少运行 `cdp-sky-check`、`cdp-comet-check`、
   `cdp-switch-check` 和 `cdp-perf-check`，确认三个长关、两个训练关与自由模式互不覆盖。
4. 当前没有现成发布包；功能交付前必须从最新 `web/` 重新同步、重打包并更新本节 SHA256。
5. 若后续需要公网分享，先确认角色模型和任何真人参考资料均可公开，再上传静态站点。
